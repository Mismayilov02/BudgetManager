package com.mismayilov.common.utility

class Util {
    companion object {
        const val DATE_FORMAT ="dd MMM yyyy"
        const val MULTILINE_DATE_FORMAT = "dd MMM'\n'yyyy"
        const val DAY_FORMAT = "dd"
        const val MONTH_FORMAT = "MMM"
        const val YEAR_FORMAT = "yyyy"
        const val TIME_FORMAT = "HH:mm"
        const val DATE_TIME_WITH_TRIM = "dd-MM-yyyy"
        const val ONE_WEEK_IN_MILLIS = 1000 * 60 * 60 * 24 * 7
    }
}