package com.mismayilov.common.utility

import android.annotation.SuppressLint
import java.text.SimpleDateFormat

class Util {
    companion object {
        const val DATE_TIME_FORMAT ="HH:mm dd MMM yyyy"
        const val DAY_FORMAT = "dd"
        const val MONTH_FORMAT = "MMM"
        const val YEAR_FORMAT = "yyyy"
    }
}